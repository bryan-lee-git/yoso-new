import React, { Component } from "react";
import { Container } from "react-materialize";

export default class ShopList extends Component {
  render() {
    return (
      <Container>
        <h1>You're on the shopping list page</h1>
      </Container>
    );
  }
}
